name: "Build emacs in flatpak"
on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '0 0 * * 6'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - apt install flatpak 

